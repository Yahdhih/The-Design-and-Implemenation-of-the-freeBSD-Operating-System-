# Jour 009 — Phase 1 · Lecture Ch. 3 §2 — Verrous : mutex, sx, rwlock

> ⏱ 60 min  |  🔁 5 min · 📖 Lecture 40 min · 🧠 15 min
> 💡 Rappel méthode : **Entrelacement** — tu fais 2 jours de lecture d'affilée ici, mais demain c'est du code. Résiste à l'envie d'enchaîner un 3ème jour de lecture : le code ancre ce que la lecture a rendu flou.

---

## 🔁 Réveil mémoire — 5 min

**Page blanche** : sans notes :
- Quelle est la différence entre un sleeping lock et un spin lock ?
- Dans quel contexte (thread kernel, interrupt handler) peut-on utiliser chacun ?
- Cite 2 types de primitives de synchronisation vus hier.

**Cartes dues** : Anki → toutes les cartes dues.

---

## 📖 Lecture — 40 min

**Lis Ch. 3 §2** (p. ___ → p. ___, voir `reference/toc.md`).

Cette section détaille les types de verrous utilisés dans le noyau FreeBSD.

### Predict-then-verify (AVANT de lire)

> *Prédiction :* Combien de types de verrous distincts FreeBSD utilise-t-il dans son noyau ? Quelle est la différence entre un mutex et un rwlock ?

> *Prédiction :* ___

### Pendant la lecture — Types de verrous à identifier

Remplis ce tableau au fil de la lecture :

| Type de verrou | Dormance possible ? | Lecteurs simultanés ? | Usage typique |
|---------------|--------------------|-----------------------|---------------|
| mutex MTX_DEF | … | … | … |
| mutex MTX_SPIN | … | … | … |
| sx lock | … | … | … |
| rwlock | … | … | … |
| lockmgr | … | … | … |

### Appui sur les sources FreeBSD

```sh
# Interfaces des verrous dans les sources :
grep -n "struct mtx" ~/freebsd-src/sys/sys/mutex.h | head -10
grep -n "struct rwlock" ~/freebsd-src/sys/sys/rwlock.h | head -10
grep -n "mtx_lock\|mtx_unlock" ~/freebsd-src/sys/kern/kern_mutex.c | head -10
```

**Question ciblée** : dans `kern_mutex.c`, que se passe-t-il quand un thread tente de prendre un mutex déjà verrouillé par un autre thread ? Boucle d'attente ou mise en sommeil ?

### Après lecture

> *Correction :* ___

---

## 🧠 Consolidation — 15 min

- **Résumé** → `reference/notes/jour-009.md` :
  - •  (tableau des types de verrous — complète-le depuis le livre)
  - •  (règle d'or : quel verrou choisir dans quel contexte)
  - •  (comment le noyau détecte les interblocages — witness(4))

- **Nouvelles cartes** (2-3) → `flashcards/cards.tsv` :
  - « Quelle est la différence entre `MTX_DEF` et `MTX_SPIN` ? Quand utiliser chacun ? » (`ch03`)
  - « Qu'est-ce qu'un sx lock (shared-exclusive lock) ? » (`ch03`)
  - « Qu'est-ce que witness(4) sous FreeBSD ? » (`ch03 tools`)

- **Schéma** : arbre décisionnel « quel verrou utiliser ? » → `reference/diagrams/jour-009-locks.txt`.

- **Question ouverte** :
  > Les verrous du noyau FreeBSD sont-ils utilisables en espace utilisateur (ex. via pthread) ? Quelle est la relation entre les verrous noyau et les pthread_mutex ?

---

## ✅ Terminé quand

- [ ] Rappel mémoire 5 min fait
- [ ] Ch. 3 §2 lu, tableau des verrous rempli
- [ ] Sources FreeBSD explorées (mutex.h ou kern_mutex.c)
- [ ] Question ciblée répondue
- [ ] Résumé dans `reference/notes/jour-009.md`
- [ ] 2-3 cartes `ch03`
- [ ] `PROGRESS.md` coché

## ↪ Demain

Ch. 3 §3 — sleep/wakeup et interruptions. Puis demain (Jour 11) : du code pour ancrer tout ça.
