# Jour 016 — Phase 1 · Lecture Ch. 4 §4 — Signaux

> ⏱ 60 min  |  🔁 5 min · 📖 Lecture 40 min · 🧠 15 min
> 💡 Rappel méthode : **Double codage** — les signaux ont une machine d'état (ignoré / par défaut / handler personnalisé) et un chemin de livraison. Dessine les deux pendant la lecture, pas après.

---

## 🔁 Réveil mémoire — 5 min

**Page blanche** : sans notes :
- Qu'est-ce que FD_CLOEXEC ? Quand est-il utile ?
- Que se passe-t-il avec les threads secondaires lors d'un exec() ?
- Cite 3 signaux POSIX standard et leur comportement par défaut.

**Cartes dues** : Anki → toutes les cartes dues.

---

## 📖 Lecture — 40 min

**Lis Ch. 4 §4** (p. ___ → p. ___, voir `reference/toc.md`).

### Predict-then-verify (AVANT de lire)

> *Prédiction :* Comment un signal passe-t-il du processus émetteur au handler du processus récepteur ? Quand est-il livré exactement ?

> *Prédiction :* ___

### Pendant la lecture — Points d'attention

**Le modèle de signaux :**
- Trois dispositions possibles : SIG_DFL, SIG_IGN, handler personnalisé.
- Quels signaux ne peuvent pas être interceptés ou ignorés ?
- Qu'est-ce qu'un signal pending vs un signal masked ?

**Le chemin de livraison :**
- Quand le signal est-il livré au processus récepteur ?
- Que se passe-t-il si le processus est en train d'exécuter un syscall bloquant (ex. : `read()`) quand il reçoit un signal ?
- EINTR : à quoi sert SA_RESTART ?

**Multi-thread et signaux :**
- Quel thread reçoit le signal dans un processus multi-thread ?
- Comment `sigprocmask()` et `pthread_sigmask()` interagissent ?

### Questions ciblées

1. Quels signaux ne peuvent être ni interceptés ni ignorés ? (2 signaux minimum)
2. Qu'est-ce qu'EINTR et comment SA_RESTART change-t-il le comportement du syscall interrompu ?

> *Réponses :*
> 1. ___
> 2. ___

Sources :
```sh
grep -n "kern_kill\|sendsig\|postsig" ~/freebsd-src/sys/kern/kern_sig.c | head -15
```

### Après lecture

> *Correction :* ___

---

## 🧠 Consolidation — 15 min

- **Résumé** → `reference/notes/jour-016.md` :
  - •  (3 dispositions possibles : SIG_DFL, SIG_IGN, handler)
  - •  (chemin de livraison : quand le signal est-il livré ?)
  - •  (EINTR et SA_RESTART)
  - •  (signaux non interceptables)

- **Nouvelles cartes** (2-3) → `flashcards/cards.tsv` :
  - « Quels sont les 2 signaux qui ne peuvent être ni interceptés ni ignorés sous POSIX ? » (`ch04`)
  - « Qu'est-ce qu'EINTR ? Que doit faire un programme correctement écrit quand read() retourne -1/EINTR ? » (`ch04`)
  - « Qu'est-ce que SA_RESTART ? Quand l'utiliser ? » (`ch04`)

- **Schéma** → `reference/diagrams/jour-016-signals.txt` :
  ```
  Émetteur (kill())
       ↓
  Noyau : marque signal pending dans struct proc
       ↓
  Prochain retour de syscall ou préemption
       ↓
  psignal() → postsig() → [handler utilisateur ou SIG_DFL/IGN]
  ```

- **Question ouverte** :
  > Qu'est-ce qu'une « signal mask stack » (sigaltstack) ? Pour quels cas d'usage ?

---

## ✅ Terminé quand

- [ ] Rappel mémoire 5 min fait
- [ ] Ch. 4 §4 lu
- [ ] 2 questions ciblées répondues
- [ ] Schéma du chemin de livraison dessiné
- [ ] Résumé dans `reference/notes/jour-016.md`
- [ ] 3 cartes `ch04`
- [ ] `PROGRESS.md` coché

## ↪ Demain

Code : mini-shell partie 1 — fork/exec/wait. Tu vas implémenter ce que tu as lu ces 3 derniers jours.
