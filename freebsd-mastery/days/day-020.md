# Jour 020 — Phase 1 · Lecture Ch. 4 §5 — Terminaison et ordonnancement ULE

> ⏱ 60 min  |  🔁 5 min · 📖 Lecture 40 min · 🧠 15 min
> 💡 Rappel méthode : **Rappel actif en début de section** — avant de lire une nouvelle section, passe 2 min à te demander ce que tu prédis. Cet effort cognitif avant la lecture augmente significativement la rétention.

---

## 🔁 Réveil mémoire — 5 min

**Page blanche** : sans notes :
- Lors d'un fork(), les struct file sont-elles copiées ou partagées ? Explique le ref counting.
- Dans kern_exec.c, l'ancien vmspace est-il libéré avant ou après le chargement du nouveau programme ?
- Qu'est-ce qu'un processus zombie ? Comment ton mini-shell l'évite-t-il ?

**Cartes dues** : Anki → toutes les cartes dues.

---

## 📖 Lecture — 40 min

**Lis Ch. 4 §5** (p. ___ → p. ___, voir `reference/toc.md`).

Cette section couvre la terminaison de processus (exit, wait) et l'ordonnanceur ULE.

### Predict-then-verify (AVANT de lire)

> *Prédiction :* Que fait le noyau exactement quand un processus appelle exit() ? Quelles ressources sont libérées et dans quel ordre ? Comment l'ordonnanceur décide-t-il quel processus exécuter ensuite ?

> *Prédiction :* ___

### Pendant la lecture — Points d'attention

**Terminaison (exit) :**
- Quelles ressources sont libérées dans exit1() ? Dans quel ordre ?
- Pourquoi le processus passe-t-il par l'état ZOMBIE avant d'être complètement détruit ?
- Qui libère la struct proc ? À quel moment ?

**Ordonnanceur ULE :**
- Architecture générale : run queues par CPU, niveaux de priorité.
- Comment le quantum (timeslice) est-il calculé ?
- Qu'est-ce que l'« interactivité » dans ULE ? Comment est-elle mesurée ?
- Différence entre processus CPU-bound et I/O-bound dans ULE.
- Équilibrage de charge entre CPUs : à quelle fréquence et selon quel critère ?

### Questions ciblées

1. Dans quel ordre exit1() libère-t-il les ressources (vmspace, fd, signaux, threads) ?
2. Pourquoi ULE distingue-t-il les processus interactifs des processus CPU-bound ? Quel avantage pour les applications UI ?

> *Réponses :*
> 1. ___
> 2. ___

### Bonus — navigation sources

```sh
grep -n "^exit1" ~/freebsd-src/sys/kern/kern_exit.c | head -5
grep -n "vmspace_exit\|fdescfree\|p_state.*ZOMBIE" ~/freebsd-src/sys/kern/kern_exit.c | head -10
grep -n "sched_choose\|tdq_runq" ~/freebsd-src/sys/kern/sched_ule.c | head -10
```

### Après lecture

> *Correction :* ___

---

## 🧠 Consolidation — 15 min

- **Résumé** → `reference/notes/jour-020.md` :
  - •  (ordre de libération des ressources dans exit1())
  - •  (pourquoi l'état ZOMBIE existe et sa durée de vie)
  - •  (ULE : run queues, interactivité, équilibrage SMP)

- **Nouvelles cartes** (2-3) → `flashcards/cards.tsv` :
  - « Pourquoi un processus passe-t-il par l'état ZOMBIE avant d'être complètement supprimé ? » (`ch04`)
  - « Qu'est-ce que l'ordonnanceur ULE ? Comment distingue-t-il processus interactifs et CPU-bound ? » (`ch04`)
  - « Dans exit1(), quelles ressources sont libérées ? La struct proc est-elle libérée immédiatement ? » (`ch04`)

- **Schéma** → `reference/diagrams/jour-020-exit-scheduling.txt` :
  - Cycle de vie processus : RUNNING → [exit1()] → ZOMBIE → [waitpid()] → DEAD
  - ULE run queue : niveaux de priorité + timeslice

- **Question ouverte** :
  > Qu'est-ce qu'un processus orphelin (dont le père est mort avant lui) ? Comment FreeBSD le gère-t-il ?

---

## ✅ Terminé quand

- [ ] Rappel mémoire 5 min fait
- [ ] Ch. 4 §5 lu
- [ ] 2 questions ciblées répondues
- [ ] Schéma cycle de vie + ULE dessiné
- [ ] Résumé dans `reference/notes/jour-020.md`
- [ ] 3 cartes `ch04`
- [ ] `PROGRESS.md` coché

## ↪ Demain

**Revue hebdomadaire** — semaine 3 : Ch. 4 complet + mini-shell. Redessine fork/exec/exit de mémoire, réexplique ULE à voix haute.
