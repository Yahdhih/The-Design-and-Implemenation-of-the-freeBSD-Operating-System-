# Jour 019 — Phase 1 · Trace — kern_fork.c, kern_exec.c, sched_ule.c

> ⏱ 60 min  |  🔁 5 min · 🔍 Trace 40 min · 🧠 15 min
> 💡 Rappel méthode : **Lire le vrai code — règle des 2 questions** — tu as 2 questions précises ci-dessous. Lis pour y répondre. Pas pour tout comprendre. La navigation descendante est la clé.

---

## 🔁 Réveil mémoire — 5 min

**Page blanche** : sans notes :
- Dessine le schéma SIGINT / mini-shell : qui ignore le signal, qui le reçoit, comment.
- Qu'est-ce que WNOHANG dans waitpid() ?
- Cite 2 choses que le noyau fait dans fork1() que tu n'avais pas anticipées avant de lire.

**Cartes dues** : Anki → toutes les cartes dues.

---

## 🔍 Trace — 40 min

### Question 1 — kern_fork.c : copie des descripteurs (20 min)

**Contexte** : dans ton mini-shell, après `fork()`, le fils hérite des descripteurs de fichiers du père. Le noyau copie la table de descripteurs. Mais comment ?

**Ta question** : dans `kern_fork.c`, quelle fonction copie la table de descripteurs ? Est-ce une copie profonde (chaque struct file est dupliquée) ou une copie légère (les struct file sont partagées et ref-comptées) ?

**Navigation** :
```sh
grep -n "fdcopy\|filedesc_copy\|fd_copy\|fdinit" ~/freebsd-src/sys/kern/kern_fork.c
# Ensuite, navigue vers la fonction trouvée :
grep -n "fdcopy" ~/freebsd-src/sys/kern/kern_descrip.c | head -10
grep -n -A 20 "^fdcopy" ~/freebsd-src/sys/kern/kern_descrip.c | head -25
```

**Réponse** :
> La fonction utilisée est `___`. Elle fait une copie ___  (légère/profonde) parce que ___.
> Les struct file sont ___ (partagées avec ref count / copiées individuellement).

---

### Question 2 — kern_exec.c : moment de libération de l'ancien vmspace (20 min)

**Contexte** : quand ton mini-shell appelle `execvp()`, l'ancien espace d'adressage (stack, heap, code) doit être remplacé par le nouveau programme. À quel moment exact ?

**Ta question** : dans `kern_exec.c`, à quel moment l'ancien vmspace est-il libéré — avant ou après que le nouveau programme soit chargé en mémoire ? Pourquoi cet ordre est-il important ?

**Navigation** :
```sh
grep -n "exec_new_vmspace\|vmspace_exec\|vmspace_free" ~/freebsd-src/sys/kern/kern_exec.c | head -10
# Cherche l'ordre : nouveau vmspace alloué AVANT ou APRÈS la libération de l'ancien ?
grep -n -B 2 -A 2 "vmspace_free\|vmspace_exec" ~/freebsd-src/sys/kern/kern_exec.c | head -30
```

**Réponse** :
> L'ancien vmspace est libéré ___ (avant/après) le chargement du nouveau programme.
> Cet ordre est important parce que ___.

---

### Bonus — sched_ule.c : sched_fork() (si temps)

```sh
grep -n -A 15 "^sched_fork" ~/freebsd-src/sys/kern/sched_ule.c | head -20
```

**Question** : qu'est-ce que `sched_fork()` initialise pour le processus fils ? Comment décide-t-il de la priorité initiale du fils ?

---

## 🧠 Consolidation — 15 min

- **Résumé** → `reference/notes/jour-019.md` :
  - •  (réponse Q1 : comment les fd sont copiés, ref counting)
  - •  (réponse Q2 : ordre libération ancien vmspace)
  - •  (ce que sched_fork() initialise)

- **Nouvelles cartes** (2-3) → `flashcards/cards.tsv` :
  - « Lors d'un fork(), les struct file sont-elles copiées ou partagées ? Comment ? » (`ch04 vfs`)
  - « Dans kern_exec.c, l'ancien vmspace est-il libéré avant ou après le chargement du nouveau programme ? » (`ch04 vm`)

- **Schéma** → `reference/diagrams/jour-019-fork-fd.txt` : relation père/fils pour les struct file après fork() (ref counting).

- **Question ouverte** :
  > Si le fils ferme un fd après fork(), cela ferme-t-il le fd dans le père ? Que se passe-t-il avec le ref count ?

---

## ✅ Terminé quand

- [ ] Question 1 répondue par lecture du code source (pas du livre)
- [ ] Question 2 répondue par lecture du code source
- [ ] Réponses notées dans `reference/notes/jour-019.md`
- [ ] 2 cartes `ch04 vfs vm` ajoutées
- [ ] `PROGRESS.md` coché

## ↪ Demain

Lecture Ch. 4 §5 — terminaison de processus et ordonnancement ULE. Puis revue hebdo Jour 21.
