# Jour 010 — Phase 1 · Lecture Ch. 3 §3 — sleep/wakeup, interruptions, timers

> ⏱ 60 min  |  🔁 5 min · 📖 Lecture 40 min · 🧠 15 min
> 💡 Rappel méthode : **Feynman** — après chaque concept clé de sleep/wakeup, ferme le livre et explique-le à voix haute. Note les mots qui manquent : ce sont tes lacunes précises.

---

## 🔁 Réveil mémoire — 5 min

**Page blanche** : sans notes :
- Remplis de mémoire le tableau des verrous (MTX_DEF, MTX_SPIN, sx, rwlock) — dormance possible ? lecteurs multiples ?
- Qu'est-ce que `witness(4)` et à quoi sert-il ?
- Quelle est la règle pour utiliser un spin lock vs un sleeping lock dans un interrupt handler ?

**Cartes dues** : Anki → toutes les cartes dues.

---

## 📖 Lecture — 40 min

**Lis Ch. 3 §3** (p. ___ → p. ___, voir `reference/toc.md`).

Cette section couvre : le mécanisme sleep/wakeup, les interruptions matérielles, les softs interrupts (ithreads, taskqueues), et les callouts (timers noyau).

### Predict-then-verify (AVANT de lire)

> *Prédiction :* Comment un thread noyau peut-il attendre un événement sans occuper le CPU ? Quel mécanisme le réveille ? Comment les interruptions matérielles s'interfacent-elles avec le scheduler ?

> *Prédiction :* ___

### Pendant la lecture — Points d'attention

**sleep/wakeup :**
- Sur quoi un thread dort-il ? (le concept de « channel »)
- Qui l'appelle pour le réveiller ? (`wakeup()` vs `wakeup_one()`)
- Quelle est la race condition classique autour de sleep/wakeup et comment FreeBSD la résout-il ?

**Interruptions :**
- Quelle est la différence entre une interruption matérielle et un ithread ?
- Pourquoi les handlers d'interruption ne peuvent-ils pas dormir ?
- Qu'est-ce qu'un taskqueue ?

**Callouts (timers) :**
- Comment planifier un callback dans N millisecondes dans le noyau ?
- Quelle est la résolution minimale du timer ?

### Questions ciblées

1. Quelle est la « lost wakeup race » et comment `tsleep()` la prévient-il ?
2. Quelle est la différence entre `wakeup()` et `wakeup_one()` ? Quand utilise-t-on l'un vs l'autre ?

> *Réponses :*
> 1. ___
> 2. ___

Sources :
```sh
grep -n "tsleep\|wakeup" ~/freebsd-src/sys/kern/kern_synch.c | head -20
```

### Après lecture

> *Correction :* ___

---

## 🧠 Consolidation — 15 min

- **Résumé** → `reference/notes/jour-010.md` :
  - •  (mécanisme sleep/wakeup — canal, tsleep, wakeup)
  - •  (interruptions → ithreads → taskqueues)
  - •  (callouts — planification de timers noyau)
  - •  (la race condition et sa solution)

- **Nouvelles cartes** (2-3) → `flashcards/cards.tsv` :
  - « Comment fonctionne tsleep() dans FreeBSD ? Qu'est-ce qu'un canal de sommeil ? » (`ch03`)
  - « Pourquoi les handlers d'interruption ne peuvent-ils pas appeler tsleep() ? » (`ch03`)
  - « Quelle est la différence entre wakeup() et wakeup_one() ? » (`ch03`)

- **Schéma** → `reference/diagrams/jour-010-sleep-wakeup.txt` :
  ```
  Thread A                      Thread B
  ---------                     ---------
  mtx_lock(lock)
  while (!condition)
    tsleep(chan, lock, ...)  →  [Thread A dors sur `chan`]
                                mtx_lock(lock)
                                condition = true
                                wakeup(chan)
                                mtx_unlock(lock)
  ← réveillé, retake lock
  mtx_unlock(lock)
  ```

- **Question ouverte** :
  > Comment le noyau gère-t-il les spurious wakeups (réveils parasites) ? Le pattern `while (!condition) tsleep(...)` est-il dans le livre ?

---

## ✅ Terminé quand

- [ ] Rappel mémoire 5 min fait
- [ ] Ch. 3 §3 lu, 2 questions ciblées répondues
- [ ] Schéma sleep/wakeup dessiné
- [ ] Résumé dans `reference/notes/jour-010.md`
- [ ] 3 cartes `ch03`
- [ ] `PROGRESS.md` coché

## ↪ Demain

Code ! fork/exec/wait en C — on concrétise tout ce qu'on a lu sur les processus et le noyau. C'est l'heure de construire.
