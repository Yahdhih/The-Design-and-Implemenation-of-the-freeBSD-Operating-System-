# Jour 013 — Phase 1 · Lecture Ch. 4 §2 — Création de processus, fork() interne

> ⏱ 60 min  |  🔁 5 min · 📖 Lecture + Trace 40 min · 🧠 15 min
> 💡 Rappel méthode : **Stratégie de lecture de code noyau** — commence par les headers (.h), descends la chaîne depuis fork1(), note chaque fonction sur papier. 2 questions ciblées avant d'entrer dans le code.

---

## 🔁 Réveil mémoire — 5 min

**Page blanche** : sans notes :
- Dessine de mémoire la structure struct proc avec 6 champs clés.
- Comment le noyau retrouve-t-il une struct proc depuis un PID ?
- Quelle est la différence entre struct proc et struct thread ?

**Cartes dues** : Anki → toutes les cartes dues.

---

## 📖 Lecture + Trace — 40 min

**Lis Ch. 4 §2** (p. ___ → p. ___, voir `reference/toc.md`).
En parallèle, navigue dans `sys/kern/kern_fork.c`.

### Predict-then-verify (AVANT de lire)

> *Prédiction :* Quelles étapes fork() doit-il effectuer pour créer un fils valide ? Dans quel ordre (struct proc, espace d'adressage, descripteurs de fichiers, threads) ?

> *Prédiction :* ___

### Questions ciblées à répondre depuis kern_fork.c

Formule tes réponses AVANT de lire, puis vérifie :

1. Dans `kern_fork.c`, dans quel ordre le noyau copie-t-il les ressources du père vers le fils ? (proc → vmspace → fd → threads — quel est l'ordre réel ?)
2. À quel moment les pages mémoire sont-elles marquées copy-on-write ? C'est dans fork1() ou dans une fonction de la couche VM ?

> *Réponses (après lecture/navigation) :*
> 1. ___
> 2. ___

### Navigation dans kern_fork.c

```sh
# Point d'entrée :
grep -n "^fork1\|^sys_fork\|^kern_fork" ~/freebsd-src/sys/kern/kern_fork.c | head -10

# Cherche la copie des descripteurs :
grep -n "fdcopy\|fdinit\|filedesc" ~/freebsd-src/sys/kern/kern_fork.c

# Cherche le COW (vmspace) :
grep -n "vmspace_fork\|vm_map_copy" ~/freebsd-src/sys/kern/kern_fork.c
```

Trace la chaîne d'appels sur papier :
```
sys_fork()
  └─► fork1()
        ├─► proc_alloc()        (alloue struct proc)
        ├─► vmspace_fork()      (copie vmspace, marque COW)
        ├─► fdcopy()            (copie table de descripteurs)
        ├─► ...
        └─► sched_fork()        (initialise scheduling fils)
```

### Après lecture

> *Correction :* ___

---

## 🧠 Consolidation — 15 min

- **Résumé** → `reference/notes/jour-013.md` :
  - •  (ordre des étapes dans fork1())
  - •  (comment le COW est mis en place via vmspace_fork())
  - •  (ce que le fils hérite et ce qu'il ne hérite pas du père)

- **Nouvelles cartes** (2-3) → `flashcards/cards.tsv` :
  - « Quelles ressources fork() copie-t-il du père vers le fils ? Dans quel ordre ? » (`ch04`)
  - « À quel moment les pages mémoire sont-elles marquées copy-on-write lors d'un fork() ? » (`ch04 vm`)

- **Schéma** → `reference/diagrams/jour-013-fork.txt` : chaîne d'appels fork() avec les structures créées à chaque étape.

- **Question ouverte** :
  > fork() est dit « rapide grâce au COW » — mais il y a quand même du travail. Quel est le coût incompressible d'un fork() indépendamment du COW ?

---

## ✅ Terminé quand

- [ ] Rappel mémoire 5 min fait
- [ ] Ch. 4 §2 lu
- [ ] kern_fork.c exploré, chaîne d'appels tracée sur papier
- [ ] 2 questions ciblées répondues
- [ ] Résumé dans `reference/notes/jour-013.md`
- [ ] 2-3 cartes `ch04 vm`
- [ ] `PROGRESS.md` coché

## ↪ Demain

**Revue hebdomadaire** — semaine 2 : Ch. 3 complet + Ch. 4 §1-2. Redessine kern_fork de mémoire, réexplique les verrous à voix haute.
