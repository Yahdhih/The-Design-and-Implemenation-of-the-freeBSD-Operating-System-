# Jour 012 — Phase 1 · Lecture Ch. 4 §1 — Structure d'un processus, struct proc

> ⏱ 60 min  |  🔁 5 min · 📖 Lecture 40 min · 🧠 15 min
> 💡 Rappel méthode : **Lire le vrai code** — avant de lire la section du livre, lis d'abord `sys/sys/proc.h`. La structure de données EST le concept. Note les champs qui te semblent importants, puis vérifie dans le livre.

---

## 🔁 Réveil mémoire — 5 min

**Page blanche** : sans notes :
- Que retourne `fork()` dans le père ? Dans le fils ? En cas d'erreur ?
- Que se passe-t-il exactement quand `execv()` est appelé (espace d'adressage, signaux, fd) ?
- Qu'est-ce qu'un processus zombie ? Comment l'éviter ?

**Cartes dues** : Anki → toutes les cartes dues (ch01-ch04).

---

## 📖 Lecture — 40 min

**Préliminaire (5 min) :** AVANT de lire le livre, ouvre les sources FreeBSD :

```sh
# Lis les champs de struct proc et struct thread :
grep -A 60 "^struct proc {" ~/freebsd-src/sys/sys/proc.h | head -70
# Ou en ligne : https://github.com/freebsd/freebsd-src/blob/main/sys/sys/proc.h
```

Note ici les 8 champs qui te semblent les plus importants :
1. `p_` ___
2. `p_` ___
3. `p_` ___
4. `p_` ___
5. `p_` ___
6. `p_` ___
7. `p_` ___
8. `p_` ___

**Maintenant lis Ch. 4 §1** (p. ___ → p. ___, voir `reference/toc.md`).

### Predict-then-verify (AVANT de lire le livre)

> *Prédiction :* Quels sont les champs les plus importants de struct proc ? Comment la relation père/fils est-elle représentée dans la structure ?

> *Prédiction :* ___

### Pendant la lecture

- **struct proc** : compare les champs que tu as notés en préliminaire avec ceux que le livre juge importants. Qu'est-ce que tu avais manqué ?
- **struct thread** : quelle est la différence avec struct proc ? Quels champs sont dans proc vs dans thread ?
- **Relation père/fils** : comment est représenté le lien de parenté entre processus ?
- **Table des processus** : comment le noyau retrouve-t-il un proc depuis un PID ?

### Après lecture

> *Correction :* ___

---

## 🧠 Consolidation — 15 min

- **Résumé** → `reference/notes/jour-012.md` :
  - •  (champs clés de struct proc et leur rôle)
  - •  (différence struct proc vs struct thread)
  - •  (comment le noyau navigue de PID → struct proc)
  - •  (relation de parenté père/fils dans les structures)

- **Nouvelles cartes** (2-3) → `flashcards/cards.tsv` :
  - « Cite 5 champs de struct proc et leur rôle. » (`ch04`)
  - « Comment le noyau FreeBSD retrouve-t-il la struct proc d'un processus depuis son PID ? » (`ch04`)

- **Schéma** → `reference/diagrams/jour-012-proc.txt` :
  ```
  struct proc
  ┌──────────────────────────────┐
  │ p_pid     : PID              │
  │ p_ppid    : PID du père      │
  │ p_vmspace : espace d'adress. │──► struct vmspace
  │ p_fd      : descripteurs     │──► struct filedesc
  │ p_ucred   : credentials      │──► struct ucred
  │ p_threads : liste de threads │──► struct thread
  │ p_state   : état             │
  └──────────────────────────────┘
  ```

- **Question ouverte** :
  > Comment les sessions et les groupes de processus (PGID, SID) sont-ils représentés par rapport à struct proc ?

---

## ✅ Terminé quand

- [ ] struct proc lu dans les sources FreeBSD AVANT le livre
- [ ] Ch. 4 §1 lu, champs clés identifiés
- [ ] Schéma struct proc dessiné
- [ ] Résumé dans `reference/notes/jour-012.md`
- [ ] 2-3 cartes `ch04`
- [ ] `PROGRESS.md` coché

## ↪ Demain

Ch. 4 §2 — la création de processus : le chemin interne de fork() dans kern_fork.c. On lit le vrai code.
