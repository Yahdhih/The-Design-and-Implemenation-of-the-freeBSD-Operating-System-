# Jour 015 — Phase 1 · Lecture Ch. 4 §3 — exec() et chargement de programme

> ⏱ 60 min  |  🔁 5 min · 📖 Lecture 40 min · 🧠 15 min
> 💡 Rappel méthode : **Predict-then-verify** — avant de lire exec(), écris le pseudo-code que tu attends de kern_execve(). Tes erreurs sont ton apprentissage le plus précieux.

---

## 🔁 Réveil mémoire — 5 min

**Page blanche** : sans notes :
- Retrace les 4 étapes principales de fork1() dans l'ordre (proc, vmspace, fd, threads).
- Cite le résultat de ton quiz de la revue : 2 erreurs à corriger en priorité.
- Comment un processus fils reçoit-il le contrôle après fork() ? Qui décide de l'ordonnancement ?

**Cartes dues** : Anki → toutes les cartes dues.

---

## 📖 Lecture — 40 min

**Lis Ch. 4 §3** (p. ___ → p. ___, voir `reference/toc.md`).

Cette section couvre exec() : comment un processus remplace son image par un nouveau programme.

### Predict-then-verify (AVANT de lire)

> *Prédiction :* Quelles ressources exec() préserve-t-il et lesquelles efface-t-il ? Que se passe-t-il avec : l'espace d'adressage, les descripteurs de fichiers, les signaux, les threads, les variables d'environnement ?

> *Prédiction :* ___

### Questions ciblées à répondre depuis le texte et kern_exec.c

1. exec() préserve-t-il les descripteurs de fichiers ouverts ? Tous ? Sous quelle condition un fd est-il fermé lors de exec() ?
2. Que se passe-t-il avec les threads secondaires (autres que le thread appelant) lors d'un exec() ?

> *Réponses :*
> 1. ___
> 2. ___

### Navigation dans les sources (5-10 min)

```sh
# Point d'entrée exec() :
grep -n "kern_execve\|exec_new_vmspace\|fdcloseexec" ~/freebsd-src/sys/kern/kern_exec.c | head -15

# Que fait exec_new_vmspace() ?
grep -n -A 5 "exec_new_vmspace" ~/freebsd-src/sys/kern/kern_exec.c | head -20
```

Trace la chaîne :
```
sys_execve()
  └─► kern_execve()
        ├─► exec_new_vmspace()    (remplace l'espace d'adressage)
        ├─► fdcloseexec()         (ferme les fd FD_CLOEXEC)
        ├─► ...
        └─► [retour vers le nouveau programme]
```

### Après lecture

> *Correction :* ___

---

## 🧠 Consolidation — 15 min

- **Résumé** → `reference/notes/jour-015.md` :
  - •  (ce qu'exec() efface : vmspace, threads secondaires, signaux…)
  - •  (ce qu'exec() préserve : PID, fd sans FD_CLOEXEC, uid/gid effectifs…)
  - •  (FD_CLOEXEC : pourquoi et comment l'utiliser)

- **Nouvelles cartes** (2-3) → `flashcards/cards.tsv` :
  - « Que se passe-t-il avec les threads secondaires lors d'un exec() ? » (`ch04`)
  - « Qu'est-ce que le flag FD_CLOEXEC et pourquoi l'utiliser ? » (`ch04`)
  - « Qu'est-ce qu'exec() efface dans l'espace d'adressage ? » (`ch04 vm`)

- **Schéma** → `reference/diagrams/jour-015-exec.txt` : avant/après exec() pour : vmspace, fd, threads, signaux.

- **Question ouverte** :
  > Pourquoi les serveurs réseau font-ils systématiquement `close(fd)` dans le fils après `fork()` ? Que se passerait-il sinon ?

---

## ✅ Terminé quand

- [ ] Rappel mémoire 5 min fait
- [ ] Ch. 4 §3 lu
- [ ] kern_exec.c navigué, 2 questions répondues
- [ ] Schéma avant/après exec() dessiné
- [ ] Résumé dans `reference/notes/jour-015.md`
- [ ] 3 cartes `ch04 vm`
- [ ] `PROGRESS.md` coché

## ↪ Demain

Ch. 4 §4 — les signaux. Comment un processus peut interrompre un autre. La fondation du job control dans le shell qu'on va coder.
